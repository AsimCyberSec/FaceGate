{
  "name": "facegate",
  "version": "1.0.0",
  "description": "FaceGate - Facial Authentication System",
  "private": true,
  "scripts": {
    "start:backend": "cd Backend && node server.js",
    "start:frontend": "cd Frontend && npm run dev",
    "setup": "cd Frontend && npm install && cd ../Backend && npm install"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.6.1"
  }
}
